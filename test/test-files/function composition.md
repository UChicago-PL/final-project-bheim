# Function Composition

Function composition combines two functions so that the output of one becomes the input of the next. In [[haskell]], the `.` operator composes functions. It is a natural tool in [[functional programming]] and works seamlessly with [[currying]] and [[partial application]].
