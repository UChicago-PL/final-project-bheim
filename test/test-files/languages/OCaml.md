# OCaml

OCaml is a functional language with a strong [[type systems|type system]] and [[type inference]]. It supports [[algebraic data types]] and [[pattern matching]], and allows both functional and imperative styles. OCaml shares many ideas with [[haskell]] but uses strict evaluation rather than [[lazy evaluation]].
