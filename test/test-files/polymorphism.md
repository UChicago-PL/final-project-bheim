# Polymorphism

Polymorphism allows functions and data types to work with values of many types. In [[haskell]], parametric polymorphism lets a single function operate over any type. It is a central feature of [[type systems]] in [[functional programming]] and enables reusable abstractions like [[higher order functions]].
