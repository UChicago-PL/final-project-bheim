# Testing

Testing verifies that programs behave correctly. In [[functional programming]], [[pure functions]] are easy to test because they have no side effects. [[haskell]] supports property-based testing through libraries like QuickCheck. Good test coverage is important for large programs.
