# Filter

Filter selects elements from a list that satisfy a predicate function. It is a fundamental [[higher order functions|higher order function]] used throughout [[functional programming]]. In [[haskell]], `filter p xs` returns only those elements of `xs` for which `p` returns true. It pairs naturally with [[map]] and [[fold]].
