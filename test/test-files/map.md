# Map

Map applies a function to every element of a list, returning a new list. It is one of the most common [[higher order functions]] in [[functional programming]]. In [[haskell]], `map f xs` applies `f` to each element of `xs`. It is closely related to [[filter]] and [[fold]].
