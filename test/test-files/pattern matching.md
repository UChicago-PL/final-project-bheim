# Pattern Matching

Pattern matching allows a function to branch based on the structure of its input. It works naturally with [[algebraic data types]] in [[haskell]]. Pattern matching is a cleaner alternative to chains of conditionals and fits well with [[recursion]].
