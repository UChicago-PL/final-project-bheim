# Currying

Currying transforms a function that takes multiple arguments into a sequence of functions each taking one argument. It is named after mathematician Haskell Curry. In [[haskell]], all functions are curried by default. Currying is closely related to [[partial application]] and [[lambda]] calculus.
