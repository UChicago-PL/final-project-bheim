# Lazy Evaluation

Lazy evaluation means expressions are not evaluated until their values are needed. [[haskell]] uses lazy evaluation by default, which allows for infinite data structures. It interacts closely with [[recursion]] and can improve performance by avoiding unnecessary computation. It contrasts with strict evaluation used in most languages.
