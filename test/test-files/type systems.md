# Type Systems

A type system classifies values and expressions by type to catch errors at compile time. [[haskell]] has a strong static type system built on [[type inference]]. Type systems in [[functional programming]] languages often include features like [[algebraic data types]] and [[polymorphism]].
