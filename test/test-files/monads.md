# Monads

Monads are a design pattern for sequencing computations in [[functional programming]]. In [[haskell]], monads are used to handle effects like state, I/O, and failure while maintaining [[pure functions|purity]]. Common monads include [[maybe monad]] and [[IO monad]]. Monads build on [[functors]] and applicatives.
