# Closures

A closure is a function that captures variables from its surrounding scope. Closures are a natural result of treating functions as first-class values in [[functional programming]]. They are related to [[lambda]] expressions and enable [[partial application]] and [[currying]].
