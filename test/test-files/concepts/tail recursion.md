# Tail Recursion

Tail recursion is a form of [[recursion]] where the recursive call is the last operation in a function. Many compilers optimize tail recursive calls to avoid stack overflow. [[haskell]] and other [[functional programming]] languages rely heavily on tail recursion alongside [[lazy evaluation]].
