# Side Effects

A side effect is any observable effect of a function beyond returning a value, such as writing to a file or mutating state. [[functional programming]] aims to minimize or isolate side effects. [[haskell]] confines side effects to the [[IO monad]], keeping the rest of the code [[pure functions|pure]] and [[referential transparency|referentially transparent]].
