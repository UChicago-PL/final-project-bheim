# Memoization

Memoization is an optimization technique that caches the results of function calls. It works best with [[pure functions]] because they always return the same result for the same input, making caching safe. Memoization is related to [[lazy evaluation]] and is commonly used with [[recursion]] to avoid redundant computation.
