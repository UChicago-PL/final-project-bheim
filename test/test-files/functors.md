# Functors

A functor is a type that can be mapped over. In [[haskell]], the `Functor` typeclass provides the `fmap` function, which is a generalization of [[map]]. Functors are a prerequisite for understanding [[monads]] and applicatives in [[functional programming]].
