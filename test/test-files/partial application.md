# Partial Application

Partial application means supplying fewer arguments to a function than it expects, producing a new function that takes the remaining arguments. It relies on [[currying]] and is common in [[haskell]] and [[functional programming]]. It works well with [[higher order functions]] and [[function composition]].
