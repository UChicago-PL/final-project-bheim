# Ord Typeclass

The Ord typeclass in [[haskell]] extends [[Eq typeclass]] with ordering operations like `<`, `>`, and `compare`. It is required for sorting and other order-dependent operations. Like Eq, it is commonly derived for [[algebraic data types]] and is part of [[haskell]]'s core [[type systems|type system]].
