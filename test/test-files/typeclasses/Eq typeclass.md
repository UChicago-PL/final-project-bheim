# Eq Typeclass

The Eq typeclass in [[haskell]] provides equality comparison via `==` and `/=`. Any type that can be compared for equality implements Eq. It is one of the most basic typeclasses in [[haskell]]'s [[type systems|type system]] and is often derived automatically using [[algebraic data types]].
