# Functor Typeclass

The Functor typeclass in [[haskell]] provides `fmap`, which applies a function over a wrapped value. It is the foundation for [[functors]] in [[haskell]]'s typeclass hierarchy. Any type that implements Functor must satisfy the functor laws and is a prerequisite for the Monad typeclass used in [[monads]].
