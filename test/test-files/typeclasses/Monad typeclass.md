# Monad Typeclass

The Monad typeclass in [[haskell]] defines `>>=` (bind) and `return`, which together sequence computations. It extends the [[Functor typeclass]] and is the basis for [[monads]] like the [[maybe monad]] and [[IO monad]]. The do-notation in [[haskell]] is syntactic sugar over monad operations.
