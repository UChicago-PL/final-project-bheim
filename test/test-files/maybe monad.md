# Maybe Monad

The Maybe monad represents computations that might fail. In [[haskell]], `Maybe a` is either `Just a` or `Nothing`. It is an [[algebraic data types|algebraic data type]] used as a [[monads|monad]] to chain computations that may produce no result. It is much safer than using null values.
