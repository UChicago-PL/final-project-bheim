# Type Inference

Type inference allows the compiler to deduce the types of expressions without explicit annotations. [[haskell]] uses the Hindley-Milner algorithm for type inference. It makes code concise while retaining the safety of a strong [[type systems|type system]].
