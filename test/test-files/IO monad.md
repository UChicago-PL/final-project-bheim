# IO Monad

The IO monad in [[haskell]] encapsulates input/output operations while keeping the rest of the language [[pure functions|pure]]. It is the main way [[haskell]] programs interact with the outside world. The IO monad is a special case of [[monads]] that cannot be escaped.
