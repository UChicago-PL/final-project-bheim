# Immutability

Immutability means that data cannot be changed after it is created. This is a key principle of [[functional programming]] and works naturally with [[pure functions]]. [[haskell]] enforces immutability by default, which makes programs easier to reason about.
